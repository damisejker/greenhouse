# Система множественных горшков для игры "Оранжерея"

## Обзор

Это обновление добавляет возможность иметь до 5 горшков одновременно вместо одного. Каждый горшок может содержать свое растение со своими уникальными характеристиками.

## Что нового

### Функциональность

1. **Множественные горшки**: Пользователи могут иметь до 5 активных горшков одновременно
2. **Переключатель горшков**: Боковая панель для быстрого переключения между горшками
3. **Выбор горшка при посадке**: При посадке семени можно выбрать, в какой горшок его посадить
4. **Независимое управление**: Каждый горшок имеет свою позицию на экране, свое растение и свои показатели
5. **Кнопка "Добавить горшок"**: PLACEHOLDER для будущего изображения кнопки

### UI/UX изменения

- Левая боковая панель показывает все горшки (🌱 = занят, ⚪ = пустой)
- Активный горшок выделен зеленым цветом
- При наведении на горшок показывается информация о растении и номер горшка
- Все действия (полив, опрыскивание, удобрение) применяются к выбранному горшку

## Структура базы данных

### Новые таблицы

#### `user_pots`
Хранит информацию о горшках каждого пользователя:
- `login` - логин пользователя
- `pot_id` - номер горшка (1-5)
- `pot_left`, `pot_top` - позиция горшка на экране
- `is_active` - активен ли горшок
- `created_at` - дата создания

#### `greenhouse_settings`
Настройки оранжереи для каждого пользователя:
- `login` - логин пользователя
- `max_pots` - максимальное количество горшков (по умолчанию 5)
- `active_pots` - текущее количество активных горшков
- `last_updated` - дата последнего обновления

### Изменения в существующих таблицах

#### `oranjerie`
Добавлено поле:
- `pot_id` (INT, NOT NULL, DEFAULT 1) - идентификатор горшка

#### `pot_positions`
Добавлено поле:
- `pot_id` (INT, NOT NULL, DEFAULT 1) - идентификатор горшка

## Миграция

### Шаг 1: Резервное копирование

**ВАЖНО!** Перед миграцией обязательно создайте резервную копию базы данных:

```bash
mysqldump -u username -p database_name > backup_before_migration.sql
```

### Шаг 2: Запуск миграции

Выполните SQL скрипт миграции:

```bash
mysql -u username -p database_name < migration_multi_pots.sql
```

Или через phpMyAdmin:
1. Откройте phpMyAdmin
2. Выберите базу данных `magismo_school`
3. Перейдите на вкладку "SQL"
4. Скопируйте содержимое файла `migration_multi_pots.sql`
5. Вставьте и выполните

### Шаг 3: Проверка миграции

После миграции проверьте:

```sql
-- Проверяем, что добавлено поле pot_id
DESCRIBE oranjerie;

-- Проверяем новые таблицы
SHOW TABLES LIKE '%pot%';
SHOW TABLES LIKE 'greenhouse_settings';

-- Проверяем данные
SELECT * FROM user_pots LIMIT 5;
SELECT * FROM greenhouse_settings LIMIT 5;
```

### Шаг 4: Тестирование

1. Войдите в систему как тестовый пользователь
2. Проверьте, что существующее растение отображается в горшке №1
3. Попробуйте добавить новый горшок
4. Посадите семя в новый горшок
5. Проверьте все функции (полив, опрыскивание, перемещение)

## Использование

### Для пользователей

1. **Переключение между горшками**: Нажмите на номер горшка в левой панели
2. **Добавление горшка**: Нажмите кнопку "➕ Добавить горшок" (PLACEHOLDER)
3. **Посадка семени**: Откройте депозитарий, выберите семя, выберите горшок из выпадающего списка
4. **Уход за растениями**: Все действия применяются к текущему выбранному горшку

### Для администраторов

#### Изменение максимального количества горшков для пользователя

```sql
UPDATE greenhouse_settings
SET max_pots = 10
WHERE login = 'username';
```

#### Просмотр всех горшков пользователя

```sql
SELECT o.pot_id, o.plant, o.health, o.water, o.plantstatus, o.stagenumber
FROM oranjerie o
WHERE o.login = 'username'
ORDER BY o.pot_id;
```

## Технические детали

### Измененные файлы

1. **migration_multi_pots.sql** - SQL скрипт миграции
2. **index.php** - основной файл с UI и логикой
3. **cycleupdate.php** - обновлен для работы с множественными горшками
4. **save_pot_position.php** - обновлен для сохранения позиций каждого горшка
5. **SQL описания таблиц** - обновлены описания

### Параметры URL

- `?pot_id=1` - отображает горшок №1
- `?pot_id=2` - отображает горшок №2
- и т.д.

### JavaScript

- Каждый горшок имеет атрибут `data-pot-id`
- При сохранении позиции передается `pot_id`
- Перетаскивание работает независимо для каждого горшка

## Отладка

### Проблемы с отображением горшков

Проверьте в консоли браузера:
```javascript
console.log(document.querySelector('.pot').getAttribute('data-pot-id'));
```

### Проблемы с сохранением позиций

Откройте Network tab в DevTools и проверьте запросы к `save_pot_position.php`

### Проблемы с базой данных

```sql
-- Проверяем целостность данных
SELECT
    o.login,
    o.pot_id,
    o.plant,
    up.pot_left,
    up.pot_top
FROM oranjerie o
LEFT JOIN user_pots up ON o.login = up.login AND o.pot_id = up.pot_id
WHERE o.login = 'username';
```

## Обратная совместимость

Миграция сохраняет все существующие данные:
- Все существующие растения автоматически получают `pot_id = 1`
- Позиции горшков мигрируют из `pot_positions` в `user_pots`
- Пользователи без настроек получают настройки по умолчанию

## TODO / Будущие улучшения

1. ✅ Добавить поддержку множественных горшков
2. ✅ Создать UI для переключения между горшками
3. ✅ Обновить все операции для работы с pot_id
4. 🔲 Добавить изображение для кнопки "Добавить горшок"
5. 🔲 Добавить возможность удалять/деактивировать горшки
6. 🔲 Добавить визуальное отображение всех горшков одновременно
7. 🔲 Добавить статистику по всем горшкам
8. 🔲 Оптимизация производительности для больших количеств горшков

## Поддержка

При возникновении проблем:
1. Проверьте логи PHP (`error_log`)
2. Проверьте консоль браузера
3. Проверьте структуру БД
4. Проверьте права доступа к файлам

## Автор

Разработано для игры "Оранжерея" на базе Магисмо
Дата: 2025-11-09
